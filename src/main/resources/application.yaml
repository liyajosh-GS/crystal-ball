spring:
  application:
    name: crystal-ball

  #  configure jdbc driver and datasource
  datasource:
    url: jdbc:postgresql://localhost/initial
    username: postgres
    password: kikkuAchu
    driver-class-name: org.postgresql.Driver

  #  configure JPA
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate.ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true

# configure oauth
  security:
    oauth2:
      client:
        registration:
          google:
            provider: google
            client-id: 1051874624760-t5od2qfid4euev455idjijqs57mfs225.apps.googleusercontent.com
            client-secret: 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
            scope: openid,profile,email


#logging
logging:
  level:
    org:
      springframework:
        security: TRACE
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: TRACE